spring:
  datasource:
    url: jdbc:mysql://localhost:3306/boottest04
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
    #     更新或者创建数据表结构
      ddl-auto: update
#    控制台显示SQL
    show-sql: true
server:
  port: 8081